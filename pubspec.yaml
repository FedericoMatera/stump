name: Stump
description: A logger for Flutter inspired by Timber.
version: 1.0.0

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  intl: ^0.17.0
  stack_trace: ^1.10.0

flutter:
